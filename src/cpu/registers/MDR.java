package cpu.registers;

/**
 * MDR (Memory Data Register) — holds data in transit to/from memory.
 * Connected directly to the Data Bus.
 */
public class MDR extends Register {
    public MDR() { super("MDR"); }
}
